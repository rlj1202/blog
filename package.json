{
  "name": "new-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "export NODE_ENV=production && next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "export NODE_ENV=development && next build && next export && touch out/.nojekyll && git add out/ && git commit -m 'Deploy to gh-pages' && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "glob": "^7.1.6",
    "gray-matter": "^4.0.2",
    "next": "11.1.1",
    "raw-loader": "^4.0.1",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "react-markdown": "^4.3.1",
    "react-syntax-highlighter": "^12.2.1"
  }
}
